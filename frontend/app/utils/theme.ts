'use client'

import { createTheme } from "@mui/material";

export const theme = createTheme({
    direction: 'rtl',
    typography: {
        fontFamily: "Ravi",
        htmlFontSize: 14,
        body1: {
            fontSize: '1rem'
        }
    },
});